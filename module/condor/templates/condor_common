CONDOR_HOST = <%= cm_list %>
COLLECTOR_NAME = <%= collector_name %>
<% unless scheduler_name.nil? or scheduler_name == "" -%>
<% if default_schedd_isha.nil? or default_schedd_isha == "" -%>
SCHEDD_HOST = <%= scheduler_name %>
<% else -%>
SCHEDD_NAME = <%= scheduler_name %>
<% end -%>
<% end -%>

DAEMON_LIST = $(DAEMON_LIST), MASTER
DC_DAEMON_LIST = $(DC_DAEMON_LIST), \
MASTER, STARTD, SCHEDD, KBDD, COLLECTOR, NEGOTIATOR, EVENTD, \
VIEW_SERVER, CONDOR_VIEW, VIEW_COLLECTOR, HAWKEYE, CREDD, HAD, \
DBMSD, QUILL, JOB_ROUTER

# Disable reporting usage information
CONDOR_DEVELOPERS = NONE
CONDOR_DEVELOPERS_COLLECTOR = NONE

# Plugin configuration
PLUGIN_DIR = $(LIBEXEC)
QMF_BROKER_HOST = <%= qmf_broker %>
QMF_BROKER_PORT = <%= qmf_port %>

# Security configuration
SEC_DEFAULT_NEGOTIATION = OPTIONAL
SEC_DEFAULT_AUTHENTICATION = OPTIONAL
SEC_DEFAULT_ENCRYPTION = REQUIRED
SEC_DEFAULT_INTEGRITY = REQUIRED

# Encryption configuration
SEC_DEFAULT_CRYPTO_METHODS = 3DES

SEC_DEFAULT_AUTHENTICATION_METHODS = CLAIMTOBE
